# scanline
gh repo create scanline --private --source=. --remote=origin --push --description "AI resume builder, optimizer, and ATS checker — build, fix, and export a CV that passes applicant tracking systems."
